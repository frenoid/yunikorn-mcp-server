apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "yunikorn-mcp-server.serviceAccountName" . }}
  labels:
    {{- include "yunikorn-mcp-server.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
